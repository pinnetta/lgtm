---
import BaseLayout from '@/layouts/base-layout.astro';
---

<BaseLayout
  title="LGTM | Page Not Found"
  description="This page doesn't exist...maybe LGTM stands for 'Lost, Gone, Totally Missing'?"
>
  <div class="not-found-root">
    <div class="not-found-bg" aria-hidden="true">
      <span class="bg-glyph bg-glyph-1">404</span>
      <span class="bg-glyph bg-glyph-2">LGTM</span>
    </div>

    <div class="container not-found-inner">
      <div class="not-found-content">
        <p class="not-found-eyebrow">Error 404</p>

        <h1 class="not-found-heading">
          Page not<br />found
        </h1>

        <p class="not-found-sub">
          <span class="not-found-mono">"Looks Good To Me"</span> — but this URL clearly didn't.
        </p>

        <p class="not-found-body">
          Whatever brought you here has vanished into the void. Try heading home or browsing all {' '}
          <strong>LGTM</strong> meanings instead.
        </p>

        <div class="not-found-actions">
          <a href="/" class="btn btn-primary">Back to home</a>
          <a href="/browse" class="btn btn-ghost">Browse all entries</a>
        </div>

        <p class="not-found-footer">
          Found a broken link?{' '}
          <a href="https://github.com/airscripts/lgtm/issues" target="_blank" rel="noopener noreferrer"
            >Open an issue on GitHub &rarr;</a
          >
        </p>
      </div>

      <!-- Do not indent this block of code. -->
      <div class="not-found-code" aria-hidden="true">
        <pre
          class="code-block"><code><span class="code-comment">// src/pages/404.astro</span>
<span class="code-kw">throw new</span> <span class="code-cls">Error</span><span class="code-punc">(</span>
  <span class="code-str">"404: page not found"</span>
<span class="code-punc">);</span>

<span class="code-comment">// suggestion:</span>
<span class="code-kw">return</span> redirect<span class="code-punc">(</span><span class="code-str">"/"</span><span class="code-punc">);</span></code></pre>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .not-found-root {
    flex: 1;
    display: flex;
    overflow: hidden;
    position: relative;
    flex-direction: column;
    background: var(--color-bg);
  }

  .not-found-bg {
    inset: 0;
    overflow: hidden;
    user-select: none;
    position: absolute;
    pointer-events: none;
  }

  .bg-glyph {
    opacity: 0.03;
    line-height: 1;
    font-weight: 900;
    position: absolute;
    white-space: nowrap;
    font-family: var(--font-mono);
  }

  .bg-glyph-1 {
    top: -2rem;
    left: -1rem;
    letter-spacing: -0.06em;
    color: var(--color-accent);
    font-size: clamp(14rem, 40vw, 28rem);
  }

  .bg-glyph-2 {
    bottom: 1rem;
    right: -1rem;
    letter-spacing: -0.04em;
    color: var(--color-text);
    font-size: clamp(6rem, 18vw, 12rem);
  }

  .not-found-inner {
    flex: 1;
    gap: 4rem;
    display: grid;
    position: relative;
    align-items: center;
    padding-block: 6rem;
    grid-template-columns: 1fr 1fr;
  }

  @media (max-width: 767px) {
    .not-found-inner {
      gap: 2.5rem;
      padding-block: 4rem;
      grid-template-columns: 1fr;
    }
  }

  .not-found-content {
    gap: 0;
    display: flex;
    flex-direction: column;
  }

  .not-found-eyebrow {
    font-weight: 700;
    font-size: 0.75rem;
    margin: 0 0 1.25rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-accent);
    font-family: var(--font-mono);
  }

  .not-found-heading {
    line-height: 1;
    font-weight: 900;
    margin: 0 0 1.5rem;
    letter-spacing: -0.04em;
    color: var(--color-text);
    font-size: clamp(3rem, 8vw, 5.5rem);
  }

  .not-found-sub {
    margin: 0 0 1rem;
    font-size: 1.0625rem;
    color: var(--color-text-muted);
  }

  .not-found-mono {
    border-radius: 4px;
    font-size: 0.9375rem;
    padding: 0.1em 0.45em;
    color: var(--color-accent);
    font-family: var(--font-mono);
    background: var(--color-accent-subtle);
  }

  .not-found-body {
    max-width: 42ch;
    line-height: 1.7;
    margin: 0 0 2rem;
    font-size: 0.9375rem;
    color: var(--color-text-muted);
  }

  .not-found-actions {
    gap: 0.75rem;
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .not-found-footer {
    margin: 0;
    font-size: 0.8125rem;
    color: var(--color-text-faint);
  }

  .not-found-footer a {
    color: var(--color-text-muted);
    border-bottom: 1px solid var(--color-border);

    transition:
      color 0.15s ease,
      border-color 0.15s ease;
  }

  .not-found-footer a:hover {
    text-decoration: none;
    color: var(--color-accent);
    border-color: var(--color-accent);
  }

  .not-found-code {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @media (max-width: 767px) {
    .not-found-code {
      justify-content: flex-start;
    }
  }

  .code-block {
    margin: 0;
    width: 100%;
    overflow: auto;
    max-width: 360px;
    line-height: 1.8;
    font-size: 0.875rem;
    border-radius: 12px;
    padding: 1.75rem 2rem;
    color: var(--color-text);
    font-family: var(--font-mono);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    box-shadow: 0 4px 24px color-mix(in srgb, var(--color-text) 6%, transparent);
  }

  .code-block code {
    font-family: inherit;
  }

  .code-comment {
    font-style: italic;
    color: var(--color-text-faint);
  }

  .code-kw {
    color: var(--color-accent);
  }

  .code-cls {
    color: var(--color-legendary);
  }

  .code-str {
    color: var(--color-rare);
  }

  .code-punc {
    color: var(--color-text-muted);
  }
</style>
