---
import '@/styles/global.css';
import { Mascot } from '@/components/mascot';
import { SITE_URL, THEME_STORAGE_KEY } from '@/lib/config';
import { ThemeSwitcher } from '@/components/theme-switcher';

type Props = {
  title?: string;
  ogTitle?: string;
  description?: string;
  canonicalUrl?: string;
  ogDescription?: string;
  mascotRarity?: 'common' | 'rare' | 'epic' | 'legendary';
};

const {
  title = 'LGTM',
  description = 'A playful catalog of alternative meanings for the famous code review acronym.',
  ogTitle,
  ogDescription,
  canonicalUrl,
  mascotRarity,
} = Astro.props;

const resolvedCanonical = canonicalUrl ?? new URL(Astro.url.pathname, SITE_URL).href;
const resolvedOgTitle = ogTitle ?? title;
const resolvedOgDesc = ogDescription ?? description;
const ogImageUrl = `${SITE_URL}/og-default.png`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="description" content={resolvedOgDesc} />
    <link rel="canonical" href={resolvedCanonical} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content={resolvedCanonical} />
    <meta property="og:title" content={resolvedOgTitle} />
    <meta property="og:description" content={resolvedOgDesc} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content="LGTM" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={resolvedOgTitle} />
    <meta name="twitter:description" content={resolvedOgDesc} />
    <meta name="twitter:image" content={ogImageUrl} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <script is:inline define:vars={{ THEME_STORAGE_KEY }}>
      (function () {
        try {
          const theme = localStorage.getItem(THEME_STORAGE_KEY);

          if (theme === 'light' || theme === 'dark') {
            document.documentElement.setAttribute('data-theme', theme);
          }
        } catch {
          // Safeguard against localStorage access issues (e.g. private mode, disabled storage).
        }
      })();
    </script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      as="style"
      rel="preload"
      onload="this.onload=null;this.rel='stylesheet'"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;600&display=swap"
    />

    <noscript>
      <link
        rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;600&display=swap"
      />
    </noscript>

    <title>{title}</title>
  </head>

  <body>
    <header
      class="sticky top-0 z-50 border-b"
      style="background: color-mix(in srgb, var(--color-bg) 85%, transparent); backdrop-filter: blur(12px); border-color: var(--color-border);"
    >
      <div class="container">
        <nav class="flex items-center justify-between gap-4" style="height: 3.75rem;">
          <a href="/" class="flex items-baseline gap-1.5 no-underline" aria-label="LGTM home">
            <span
              class="text-lg font-semibold tracking-tight"
              style="font-family: var(--font-mono); color: var(--color-accent); letter-spacing: -0.02em;"
            >
              LGTM
            </span>
          </a>

          <div class="hidden sm:flex items-center gap-1.5">
            <a
              href="/browse"
              style="color: var(--color-text-muted);"
              onmouseout="this.style.color='var(--color-text-muted)';this.style.background='';"
              class="text-[0.9375rem] font-medium px-3 py-1.5 rounded-md no-underline transition-colors duration-150"
              onmouseover="this.style.color='var(--color-text)';this.style.background='var(--color-surface-raised)';"
            >
              Browse
            </a>

            <a
              href="/categories"
              style="color: var(--color-text-muted);"
              onmouseout="this.style.color='var(--color-text-muted)';this.style.background='';"
              class="text-[0.9375rem] font-medium px-3 py-1.5 rounded-md no-underline transition-colors duration-150"
              onmouseover="this.style.color='var(--color-text)';this.style.background='var(--color-surface-raised)';"
            >
              Categories
            </a>

            <a
              href="/rarities"
              style="color: var(--color-text-muted);"
              onmouseout="this.style.color='var(--color-text-muted)';this.style.background='';"
              class="text-[0.9375rem] font-medium px-3 py-1.5 rounded-md no-underline transition-colors duration-150"
              onmouseover="this.style.color='var(--color-text)';this.style.background='var(--color-surface-raised)';"
            >
              Rarities
            </a>

            <a href="/random" class="btn-nav-random mr-1">Random</a>
            <ThemeSwitcher client:load />
          </div>

          <div class="flex sm:hidden items-center gap-2">
            <ThemeSwitcher client:load />

            <button
              id="nav-toggle"
              class="nav-hamburger"
              aria-expanded="false"
              aria-controls="mobile-menu"
              aria-label="Toggle navigation"
            >
              <span class="bar bar-top"></span>
              <span class="bar bar-mid"></span>
              <span class="bar bar-bot"></span>
            </button>
          </div>
        </nav>
      </div>

      <div id="mobile-menu" class="mobile-menu sm:hidden" aria-hidden="true">
        <div class="container">
          <div class="mobile-menu-inner">
            <a href="/browse" class="mobile-nav-link">Browse</a>
            <a href="/categories" class="mobile-nav-link">Categories</a>
            <a href="/rarities" class="mobile-nav-link">Rarities</a>
            <a href="/random" class="mobile-nav-random">Random</a>
          </div>
        </div>
      </div>
    </header>

    <main class="flex-1 flex flex-col">
      <slot />
    </main>

    <footer class="footer-root border-t" style="border-color: var(--color-border);">
      <div class="container">
        <div class="footer-top">
          <div class="footer-brand">
            <span class="footer-logo" style="font-family: var(--font-mono); color: var(--color-accent);">LGTM</span>

            <p class="footer-tagline" style="color: var(--color-text-muted);">
              A playful catalog of alternative meanings for the famous code review acronym.
            </p>
          </div>

          <div class="footer-nav-cols">
            <div class="footer-nav-col">
              <span class="footer-col-heading" style="color: var(--color-text-faint);"> Explore </span>
              <a href="/browse" class="footer-link" style="color: var(--color-text-muted);"> Browse All </a>
              <a href="/categories" class="footer-link" style="color: var(--color-text-muted);"> Categories </a>
              <a href="/rarities" class="footer-link" style="color: var(--color-text-muted);"> Rarities </a>
            </div>

            <div class="footer-nav-col">
              <span class="footer-col-heading" style="color: var(--color-text-faint);"> Discover </span>
              <a href="/random" class="footer-link" style="color: var(--color-text-muted);"> Random Entry </a>

              <a class="footer-link" href="/rarities/legendary" style="color: var(--color-text-muted);">
                Legendary Tier
              </a>

              <a href="/rarities/epic" class="footer-link" style="color: var(--color-text-muted);"> Epic Tier </a>
            </div>
          </div>
        </div>

        <div class="footer-divider" style="background: var(--color-border);"></div>

        <div class="footer-bottom">
          <p class="footer-copy" style="color: var(--color-text-faint);">
            &copy; {new Date().getFullYear()} LGTM. Star on
            <a
              target="_blank"
              class="footer-ext-link"
              rel="noopener noreferrer"
              style="color: var(--color-text-muted);"
              href="https://github.com/airscripts/lgtm"
            >
              GitHub
            </a>.
          </p>

          <p class="footer-copy" style="color: var(--color-text-faint);">
            Built by
            <a
              target="_blank"
              class="footer-ext-link"
              rel="noopener noreferrer"
              href="https://airscript.it"
              style="color: var(--color-text-muted);">Airscript</a
            >.
          </p>
        </div>
      </div>
    </footer>

    <script>
      const toggle = document.getElementById('nav-toggle');
      const menu = document.getElementById('mobile-menu');

      toggle?.addEventListener('click', () => {
        const open = toggle.getAttribute('aria-expanded') === 'true';
        toggle.setAttribute('aria-expanded', String(!open));
        menu?.setAttribute('aria-hidden', String(open));
        toggle.classList.toggle('is-open', !open);
        menu?.classList.toggle('is-open', !open);
      });

      menu?.querySelectorAll('a').forEach((a) => {
        a.addEventListener('click', () => {
          toggle?.setAttribute('aria-expanded', 'false');
          menu.setAttribute('aria-hidden', 'true');
          toggle?.classList.remove('is-open');
          menu.classList.remove('is-open');
        });
      });
    </script>

    <script>
      import { showEasterEgg } from '@/lib/easter-egg';
      showEasterEgg();
    </script>

    <Mascot client:only="react" rarity={mascotRarity} isIndex={Astro.url.pathname === '/'} />
  </body>
</html>

<style>
  .btn-nav-random {
    border: none;
    gap: 0.375rem;
    color: #fff;
    cursor: pointer;
    font-weight: 600;
    line-height: 1.5;
    border-radius: 6px;
    align-items: center;
    white-space: nowrap;
    display: inline-flex;
    font-size: 0.9375rem;
    text-decoration: none;
    padding: 0.375rem 0.875rem;
    background: var(--color-accent);

    transition:
      background 0.15s ease,
      transform 0.1s ease,
      box-shadow 0.15s ease;
  }

  .btn-nav-random:hover {
    color: #fff;
    text-decoration: none;
    transform: translateY(-1px);
    background: var(--color-accent-hover);
    box-shadow: 0 4px 12px color-mix(in srgb, var(--color-accent) 40%, transparent);
  }

  .btn-nav-random:active {
    transform: translateY(0);
  }

  .nav-hamburger {
    gap: 5px;
    width: 2rem;
    height: 2rem;
    border: none;
    display: flex;
    cursor: pointer;
    padding: 0.25rem;
    border-radius: 6px;
    flex-direction: column;
    justify-content: center;
    background: transparent;
    transition: background 0.15s ease;
  }

  .nav-hamburger:hover {
    background: var(--color-surface-raised);
  }

  .bar {
    width: 100%;
    height: 2px;
    display: block;
    border-radius: 2px;
    transform-origin: center;
    background: var(--color-text-muted);

    transition:
      transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
      opacity 0.25s ease,
      width 0.25s ease;
  }

  .nav-hamburger.is-open .bar-top {
    transform: translateY(7px) rotate(45deg);
  }

  .nav-hamburger.is-open .bar-mid {
    opacity: 0;
    transform: scaleX(0);
  }

  .nav-hamburger.is-open .bar-bot {
    transform: translateY(-7px) rotate(-45deg);
  }

  .mobile-menu {
    opacity: 0;
    max-height: 0;
    overflow: hidden;
    border-top: 1px solid transparent;

    transition:
      max-height 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      opacity 0.25s ease;
  }

  .mobile-menu.is-open {
    opacity: 1;
    max-height: 22rem;
    border-top-color: var(--color-border);
  }

  .mobile-menu-inner {
    display: flex;
    gap: 0.125rem;
    flex-direction: column;
    padding: 0.75rem 0 1rem;
  }

  .mobile-nav-link {
    display: block;
    font-size: 1rem;
    font-weight: 500;
    border-radius: 8px;
    text-decoration: none;
    padding: 0.625rem 0.5rem;
    color: var(--color-text-muted);

    transition:
      color 0.15s ease,
      background 0.15s ease;
  }

  .mobile-nav-link:hover {
    text-decoration: none;
    color: var(--color-text);
    background: var(--color-surface-raised);
  }

  .mobile-nav-random {
    color: #fff;
    font-weight: 600;
    margin-top: 0.5rem;
    border-radius: 8px;
    align-items: center;
    display: inline-flex;
    font-size: 0.9375rem;
    text-decoration: none;
    align-self: flex-start;
    padding: 0.5rem 1.25rem;
    background: var(--color-accent);

    transition:
      background 0.15s ease,
      transform 0.1s ease;
  }

  .mobile-nav-random:hover {
    color: #fff;
    text-decoration: none;
    transform: translateY(-1px);
    background: var(--color-accent-hover);
  }

  .footer-root {
    padding: 3rem 0 1.75rem;
  }

  .footer-top {
    gap: 3rem;
    display: grid;
    align-items: start;
    grid-template-columns: 1fr auto;
  }

  @media (max-width: 639px) {
    .footer-top {
      gap: 2rem;
      grid-template-columns: 1fr;
    }
  }

  .footer-brand {
    display: flex;
    gap: 0.625rem;
    max-width: 320px;
    flex-direction: column;
  }

  .footer-logo {
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .footer-tagline {
    margin: 0;
    line-height: 1.6;
    font-size: 0.8125rem;
  }

  .footer-nav-cols {
    gap: 3rem;
    display: flex;
  }

  @media (max-width: 639px) {
    .footer-nav-cols {
      gap: 2rem;
    }
  }

  .footer-nav-col {
    gap: 0.625rem;
    display: flex;
    flex-direction: column;
  }

  .footer-col-heading {
    font-weight: 700;
    font-size: 0.6875rem;
    letter-spacing: 0.1em;
    margin-bottom: 0.25rem;
    text-transform: uppercase;
  }

  .footer-link {
    font-size: 0.875rem;
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .footer-link:hover {
    text-decoration: none;
    color: var(--color-text) !important;
  }

  .footer-divider {
    height: 1px;
    margin: 2rem 0 1.25rem;
  }

  .footer-bottom {
    gap: 0.5rem;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
  }

  .footer-copy {
    margin: 0;
    font-size: 0.8125rem;
  }

  .footer-ext-link {
    text-decoration: none;
    border-bottom: 1px solid var(--color-border);

    transition:
      color 0.15s ease,
      border-color 0.15s ease;
  }

  .footer-ext-link:hover {
    text-decoration: none;
    color: var(--color-text) !important;
    border-color: var(--color-text-muted);
  }
</style>
